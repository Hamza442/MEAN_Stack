//this file is to under stand modules
//in this we will pass an array and count the string length
var count = function( arr){
    return 'There are '+arr.length+' elements in array';
};
module.exports=count;
//module.exports=counter is returning counter into that module which has required this member